import { Link } from 'react-router-dom'

export function GuidePage({ advanced = false }: { advanced?: boolean }) {
  return <main className="page-shell prose-page guide-page"><p className="eyebrow">{advanced ? 'ADVANCED MECHANISM NOTE' : 'BEGINNER GUIDE'}</p><h1>{advanced ? 'What happens after your prediction' : 'Read the fire premium'}</h1>{advanced ? <><p>Moving a slider buys exposure in the direction of your estimate. The LMSR computes the exact share change as b × (logit(target) − logit(current)); the contract enforces a 1% maximum-cost boundary.</p><h2>Redemption math</h2><p>For a band [low, high], higher exposure pays 0 below low, (value − low)/(high − low) inside the band, and 1 above high. Lower exposure pays the complement.</p><h2>Why “close” can still lose</h2><p>Suppose the market said $100, you moved it to $105, and the stock settled at $103. Your prediction was close, but it moved the shared estimate away from the truth. You lose; the traders who moved it back profit. Direction relative to the prior market—not applause for an isolated guess—determines P&amp;L.</p></> : <><p>Each company shows two dollar estimates. One assumes the CEO leaves by the horizon; the other assumes the CEO stays. Their gap is the fire premium.</p><p>Drag a scenario slider to the stock price you expect under that condition. A budget caps how far the automated market can move. You never need to choose a token in the primary flow.</p><h2>The safety rule</h2><p>Only one conditional scenario can count. The other is called off, and its paid-in cash is refunded exactly. That branch creates no profit and no loss.</p><Link to="/mechanism">Read the advanced mechanism note →</Link></>}</main>
}
